services:
  - type: web
    name: finanzas-personales
    env: python
    plan: free
    region: oregon
    branch: claude/personal-finance-app-3gk1h6
    rootDir: finanzas
    buildCommand: pip install -r requirements.txt
    startCommand: python webapp.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
